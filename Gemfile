source 'https://rubygems.org'

ruby '2.1.3'

# this is a SINATRA project. we use SINATRA
gem 'sinatra'

# use json gem, because this is a web API
gem 'json'

# use Sequel for database storage
gem 'sequel'
